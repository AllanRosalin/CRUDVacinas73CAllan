@page
@model CRUDVacinas73CAllan.Pages.Vacinas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vacina[0].lote)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vacina[0].Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vacina[0].Datafabricacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vacina[0].Datavencimento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Vacina) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.lote)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datafabricacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datavencimento)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
